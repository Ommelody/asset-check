# ไฟล์ที่ต้องอัปโหลดขึ้น GitHub (ทั้งหมด 8 ไฟล์)

อัปโหลดให้ครบทุกไฟล์/โฟลเดอร์นี้ **ไว้ที่ระดับเดียวกัน** (root ของ repo)

```
index.html                    ← ตัวแอป (มีข้อมูลครุภัณฑ์ฝังอยู่แล้ว)
manifest.json                 ← ทำให้ติดตั้งเป็นแอปได้ (PWA)
sw.js                         ← ทำให้เปิดใช้ตอนออฟไลน์ได้
icons/icon-192.png
icons/icon-512.png
icons/icon-512-maskable.png
icons/apple-touch-icon.png
icons/favicon-32.png
icons/favicon-16.png
images/                       ← (ถ้ามี) รูปครุภัณฑ์ ตั้งชื่อตามรหัสกลุ่ม เช่น PCH-14.jpg
```

> ⚠️ **ต้องอัปครบ** ถ้าขาด `manifest.json` หรือ `sw.js` จะติดตั้งเป็นแอปไม่ได้

---

## วิธีติดตั้งเป็นแอปบนมือถือ

**Android (Chrome)**
1. เปิดเว็บ → กดปุ่ม ⋮ มุมขวาบน
2. เลือก **ติดตั้งแอป** (หรือ "เพิ่มลงในหน้าจอหลัก")
3. ไอคอนจะไปอยู่หน้าจอหลัก เปิดแล้วเต็มจอ ไม่มีแถบเบราว์เซอร์

**iPhone / iPad (ต้องใช้ Safari)**
1. เปิดเว็บใน **Safari** (Chrome บน iOS ติดตั้งไม่ได้)
2. กดปุ่ม **แชร์** (สี่เหลี่ยมลูกศรขึ้น) ด้านล่าง
3. เลื่อนหา **เพิ่มไปยังหน้าจอโฮม** → เพิ่ม

**คอมพิวเตอร์ (Chrome / Edge)**
- กดไอคอน **⊕ ติดตั้ง** ในแถบที่อยู่ (ขวาสุด) → ติดตั้ง

---

## หมายเหตุสำคัญ

- **ต้องเป็น https** — GitHub Pages เป็น https อยู่แล้ว ใช้ได้เลย (PWA และกล้องไม่ทำงานบน http)
- **อัปเดตแอปครั้งต่อไป:** อัปทับ `index.html` แล้วเปิดแอป ระบบจะดึงเวอร์ชันใหม่ให้เองและรีเฟรช 1 ครั้ง
  ถ้าไม่อัปเดต ให้ปิดแอปแล้วเปิดใหม่ หรือกด Ctrl+F5 บนเบราว์เซอร์
- **URL Google Sheet ฝังมาให้แล้ว** — ทุกเครื่องที่เปิดแอปเห็นข้อมูลชุดเดียวกันทันที ไม่ต้องตั้งค่าอะไร
